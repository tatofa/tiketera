'use client';
import { Store } from '@/lib/store';

export default function ReportsPage(){function exportCsv(){const orders=Store.orders();const rows=[['id','buyerName','buyerEmail','status','totalAmount','currency','createdAt'],...orders.map(o=>[o.id,o.buyerName,o.buyerEmail,o.status,String(o.totalAmount),o.currency,o.createdAt])];const csv=rows.map(r=>r.map(v=>`"${String(v).replaceAll('"','""')}"`).join(',')).join('\n');const blob=new Blob([csv],{type:'text/csv'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download='ventas-ticketera.csv';a.click();URL.revokeObjectURL(url)}return <section className="container-page py-10"><h1 className="text-4xl font-black">Reportes</h1><div className="card mt-8 max-w-xl p-6"><h2 className="text-xl font-black">Exportación de ventas</h2><p className="mt-2 text-slate-600">Descarga CSV con órdenes registradas en modo demo.</p><button className="btn-primary mt-6" onClick={exportCsv}>Exportar CSV</button></div></section>}
