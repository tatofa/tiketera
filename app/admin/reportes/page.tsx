import { accreditationStats, formatMoney, getSalesByRrpp, getTotals, salesRows } from '@/lib/platform-config';

export default function ReportesPage(){
 const totals=getTotals();
 const rrpp=getSalesByRrpp();
 return <section className="container-page py-10"><p className="font-semibold text-brand-700">Reportes</p><h1 className="text-4xl font-black">Ventas generales y segmentadas</h1><p className="mt-2 text-slate-600">Resumen por evento, canal, fecha, RRPP, cargos de servicio y acreditación.</p><div className="mt-8 grid gap-4 md:grid-cols-4"><div className="card p-5"><p className="text-sm text-slate-500">Tickets</p><p className="text-3xl font-black">{totals.tickets}</p></div><div className="card p-5"><p className="text-sm text-slate-500">Bruto</p><p className="text-3xl font-black">{formatMoney(totals.gross)}</p></div><div className="card p-5"><p className="text-sm text-slate-500">Cargos</p><p className="text-3xl font-black">{formatMoney(totals.serviceFees)}</p></div><div className="card p-5"><p className="text-sm text-slate-500">Neto</p><p className="text-3xl font-black">{formatMoney(totals.net)}</p></div></div><div className="mt-8 card p-6"><h2 className="text-xl font-black">Detalle de ventas</h2><div className="mt-4 overflow-x-auto"><table className="min-w-full text-sm"><thead><tr className="border-b text-left"><th className="p-3">Fecha</th><th className="p-3">Evento</th><th className="p-3">Canal</th><th className="p-3">RRPP</th><th className="p-3">Tickets</th><th className="p-3">Bruto</th><th className="p-3">Cargos</th><th className="p-3">Neto</th></tr></thead><tbody>{salesRows.map(row=><tr key={row.id} className="border-b"><td className="p-3">{row.date}</td><td className="p-3 font-medium">{row.eventName}</td><td className="p-3">{row.channel}</td><td className="p-3 font-mono">{row.rrppCode??'—'}</td><td className="p-3">{row.tickets}</td><td className="p-3">{formatMoney(row.gross)}</td><td className="p-3">{formatMoney(row.serviceFees)}</td><td className="p-3 font-bold">{formatMoney(row.net)}</td></tr>)}</tbody></table></div></div><div className="mt-8 grid gap-6 lg:grid-cols-2"><div className="card p-6"><h2 className="text-xl font-black">Ranking RRPP</h2><div className="mt-4 space-y-3">{rrpp.map(row=><div key={row.code} className="flex justify-between rounded-xl bg-slate-50 p-3"><span className="font-mono">/{row.code}</span><strong>{row.tickets} tickets · {formatMoney(row.gross)}</strong></div>)}</div></div><div className="card p-6"><h2 className="text-xl font-black">Acreditación</h2><div className="mt-4 space-y-3">{accreditationStats.map(row=><div key={row.eventId} className="rounded-xl bg-slate-50 p-3"><p className="font-bold">{row.eventName}</p><p className="text-sm text-slate-600">{row.gate}: {row.valid} válidos, {row.rejected} rechazados, {row.duplicated} duplicados</p></div>)}</div></div></div></section>
}
