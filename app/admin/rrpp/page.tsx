import Link from 'next/link';
import { formatMoney, getSalesByRrpp, promoterLinks } from '@/lib/platform-config';

export default function RrppPage(){
 const rows=getSalesByRrpp();
 return <section className="container-page py-10"><p className="font-semibold text-brand-700">Promotores</p><h1 className="text-4xl font-black">RRPP y links propios</h1><p className="mt-2 text-slate-600">Cada RRPP tiene link trackeable, comisión y reporte de ventas propio.</p><div className="mt-8 grid gap-4 lg:grid-cols-3">{promoterLinks.map(link=><div key={link.id} className="card p-6"><div className="flex items-start justify-between gap-3"><div><h2 className="text-xl font-black">{link.name}</h2><p className="text-sm text-slate-600">Código: <span className="font-mono">{link.code}</span></p></div><span className="badge">{link.active?'Activo':'Pausado'}</span></div><p className="mt-4 rounded-xl bg-slate-50 p-3 font-mono text-sm">{link.url}</p><p className="mt-3 text-sm text-slate-700">Comisión: {link.commissionType==='percent'?`${link.commissionValue}%`:formatMoney(link.commissionValue)}</p><Link href={link.url} className="btn-secondary mt-5 w-full justify-center">Abrir link</Link></div>)}</div><div className="mt-8 card p-6"><h2 className="text-2xl font-black">Ventas por RRPP</h2><div className="mt-4 overflow-x-auto"><table className="min-w-full text-sm"><thead><tr className="border-b text-left"><th className="p-3">RRPP</th><th className="p-3">Tickets</th><th className="p-3">Bruto</th><th className="p-3">Cargos</th><th className="p-3">Neto</th></tr></thead><tbody>{rows.map(row=><tr key={row.code} className="border-b"><td className="p-3 font-mono">/{row.code}</td><td className="p-3">{row.tickets}</td><td className="p-3">{formatMoney(row.gross)}</td><td className="p-3">{formatMoney(row.serviceFees)}</td><td className="p-3 font-bold">{formatMoney(row.net)}</td></tr>)}</tbody></table></div></div></section>
}
